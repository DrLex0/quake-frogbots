fraglimit 0
timelimit 10
teamplay 2
deathmatch 3
maxplayers 4
hostname "Frogbot Clan Arena"

// Requires engine with custom cvar support (i.e., most modern engines)
set add_q_aerowalk	1	// quad in map Aerowalk
set deathmatch 3	// vkQuake (and possibly other engines) workaround for first load

// TODO may allow loading these from custom cvars as well
// localinfo k_timetop		30	// maximum time in minutes allocateable by a player for a game
// localinfo k_overtime		1	// overtime mode (0 = off, 1 = time, 2 = SD)
// localinfo k_exttime		3	// overtime length if overtime == 1
// localinfo allow_startsound	0	// start sound mode (0 = off, 1 = on)
// localinfo k_count			10	// number of seconds for the countdown to last
// localinfo autoreport		1	// auto team report for human (0 = off, 1 = on)
// localinfo k_kfp			1	// "kill" flood protection

// Game parameters:
// ENABLE_POWERUPS + MATCH + LAVA_CHEAT + ENABLE_DROPWEAP + ENABLE_AUTOREPORT + ENABLE_AUTOSTEAMS + ENABLE_BOTCHAT + BOTJUMP
samelevel 8396609	// autoteams on
// ENABLE_POWERUPS + MATCH + LAVA_CHEAT + ENABLE_DROPWEAP + ENABLE_AUTOREPORT + ENABLE_BOTCHAT + BOTJUMP
//samelevel 8394561	// autoteams off

echo "Frogbot 2on2 cfg loaded"

map aerowalk

// must set skill after map change, because engine may force it to 1-3 range
skill 5	// bot skill 0-20
